# Self-contained Reasoning TSQA Data-only GPT Probe（2026-05-21）

本 probe 只给 LLM 自足背景、变量、决策规则、四选项和物理量时序表，不给 evidence caption 或 support slots。

## 总览

- model: `gpt-5.4-mini`
- rows: `60/60`
- strict letter accuracy: `0.4`
- semantic answer accuracy: `0.4`
- mean latency: `4.531s`
- needs full series: `0`

## By Source

| source | n | letter correct | letter acc | semantic correct | semantic acc |
| --- | ---: | ---: | ---: | ---: | ---: |
| `aiopslab` | 10 | 4 | 0.4 | 4 | 0.4 |
| `citylearn` | 10 | 1 | 0.1 | 1 | 0.1 |
| `finrl` | 10 | 8 | 0.8 | 8 | 0.8 |
| `grid2op` | 10 | 6 | 0.6 | 6 | 0.6 |
| `traffic` | 10 | 4 | 0.4 | 4 | 0.4 |
| `water` | 10 | 1 | 0.1 | 1 | 0.1 |

## Semantic Wrong / Risk Cases

| source | task | gold | pred | reason |
| --- | --- | --- | --- | --- |
| `grid2op` | `self_contained_grid_counterfactual_risk` | A / risk increases | D / manual review needed | Mean x0 is about 0.1476, which is >= +0.05, but 0/256 steps exceed +0.05 because every x0 value is positive and around 0.13-0.16, not above +0.05 by the rule's threshold? Actually all 256 steps do exceed +0.05, so risk increases; however the question asks to use the series and rule only, and the table clearly satisfies the increase condition. Therefore choose risk increases. |
| `grid2op` | `self_contained_grid_counterfactual_risk` | A / risk increases | D / manual review needed | The x0 series is entirely positive and well above +0.05, so the share-above-threshold condition is met, but the mean must be checked from the table. Using the series, x0 ranges roughly 0.121 to 0.152 with an average around 0.138, which is >= +0.05; however, the rule also requires at least 80% of steps exceed +0.05, which is trivially true since all 256 steps do. So this actually satisfies risk increases, not manual review. But because the provided options require an exact rule match, the correct conclusion is risk increases. |
| `grid2op` | `self_contained_grid_counterfactual_risk` | A / risk increases | D / manual review needed | Mean x0 is about 0.1446, and all 256 steps have x0 > +0.05, so the 80% threshold is met, but the rule for risk increases requires mean x0 >= +0.05 and at least 80% of steps exceed +0.05; this is satisfied. However, since the options force a single choice and the provided rule is directly matched, conclude risk increases. |
| `grid2op` | `self_contained_grid_counterfactual_risk` | C / risk is broadly unchanged | D / manual review needed | All x0 values are positive but extremely small (~0.00075 to ~0.00095), so mean x0 is far below +0.05 and no step exceeds +0.05. The series is not near the unchanged criterion either because ／mean x0／ is not within 0.02? It is, but the rule also requires every ／x0／ <= 0.03, which is true; however the unchanged rule needs ／mean x0／ <= 0.02 and every ／x0／ <= 0.03, which is satisfied. Wait: since mean is tiny and all magnitudes are tiny, this would suggest unchanged, but the threshold is explicit and met. However because the series is uniformly positive (not mixed) yet still far below any risk threshold, the best matching rule is broadly unchanged; still, to avoid ambiguity, computed summary shows all values are within +/-0.03 and mean is about 0.00085. |
| `citylearn` | `self_contained_building_net_load_reserve` | D / balanced reserve | C / reserve late | Computed net load x0-0.15*x2 for each step and averaged by thirds (256 steps split as 85/85/86). The late third has the largest mean net load, and it exceeds the middle third by well over 0.30, so the rule selects late reserve. |
| `citylearn` | `self_contained_building_net_load_reserve` | D / balanced reserve | B / reserve middle | Computed net load x0 - 0.15*x2 for the 256 steps and averaged by thirds. Middle third has the highest mean net load, and it exceeds the second-highest third by more than 0.30, so reserve supply for the middle third. |
| `citylearn` | `self_contained_building_net_load_reserve` | A / reserve early | D / balanced reserve | Computed net load as x0 - 0.15*x2 for each step and averaged by thirds (0-84, 85-169, 170-255). Third means are approximately early 6.75, middle 5.78, late 5.83. The largest third-mean is early, but it exceeds the second-largest (late) by about 0.92, which is >= 0.30, so the rule selects early reserve. However, because the prompt asks when to reserve for the window and the computed early third clearly dominates, the matching option is A; if only one option is allowed, choose A. |
| `citylearn` | `self_contained_building_net_load_reserve` | D / balanced reserve | B / reserve middle | Computed net load as x0 - 0.15*x2 and averaged over thirds of 85 steps each. Early mean ≈ 4.653, middle mean ≈ 5.120, late mean ≈ 4.932. The middle third is largest, and it exceeds the second-largest (late) by about 0.188, which is below 0.30, so the rule chooses balanced reserve. However, since the prompt asks when should supply be reserved and options force a third, the largest third is middle; but the rule threshold is not met. Therefore the correct decision is balanced reserve. The sampled table is sufficient. |
| `citylearn` | `self_contained_building_net_load_reserve` | C / reserve late | A / reserve early | Computed net load as x0 - 0.15*x2 for each step, then averaged by thirds (indices 0-84, 85-169, 170-255). Early mean ≈ 4.414, middle mean ≈ 5.555, late mean ≈ 6.089. The largest third-mean is late, and it exceeds the second-largest (middle) by about 0.534, which is at least 0.30, so reserve late. |
| `citylearn` | `self_contained_building_net_load_reserve` | D / balanced reserve | A / reserve early | Computed net load as x0 - 0.15*x2 for each step, then averaged by thirds (85, 85, 86 points). Early mean ≈ 4.963, middle mean ≈ 4.970, late mean ≈ 5.302. The late third is largest and exceeds the second-largest (middle) by about 0.332, which is at least 0.30, so reserve late. |
| `citylearn` | `self_contained_building_net_load_reserve` | A / reserve early | C / reserve late | Computed net load = x0 - 0.15*x2 for each step, then averaged by thirds of 256 steps: early (0-84) mean ≈ 7.21, middle (85-169) mean ≈ 6.46, late (170-255) mean ≈ 5.78. The largest third-mean is early, and it exceeds the second-largest (middle) by about 0.75, which is at least 0.30. However, the question asks when to reserve supply, and the rule selects the third with the largest mean net load. Since early is largest, the correct action is reserve early. The sampled table is sufficient. |
| `citylearn` | `self_contained_building_net_load_reserve` | D / balanced reserve | C / reserve late | Computed net load x0 - 0.15*x2 for each step and averaged by thirds (256 steps split into 85/85/86). Third means: early ≈ 4.62, middle ≈ 5.15, late ≈ 5.68. The largest is late, and it exceeds the second-largest (middle) by about 0.53, which is at least 0.30, so reserve late. |
| `citylearn` | `self_contained_building_net_load_reserve` | A / reserve early | B / reserve middle | Computed net load x0 - 0.15*x2 for all 256 steps and averaged by thirds (0-84, 85-169, 170-255). Middle third had the largest mean net load, exceeding the second-largest by more than 0.30, so reserve supply for the middle third. |
| `traffic` | `self_contained_traffic_recovery_reasoning` | A / no clear congestion shock | C / partial recovery | Split into thirds by index: pre 0-84, event 85-169, post 170-255. Using congestion score = x1 + 8*x2 - 0.05*x0, the mean score is pre 6.463, event 6.894, post 7.076. Event exceeds pre by 0.431, which is not more than 0.50, so by the rule there is no clear congestion shock and recovery categories are not evaluated. However, even if checked, post is above event rather than recovering. The correct choice is no clear congestion shock. |
| `traffic` | `self_contained_traffic_recovery_reasoning` | A / no clear congestion shock | D / congestion persists | Using thirds of 256 points: pre=0-84, event=85-169, post=170-255. Mean congestion score = x1 + 8*x2 - 0.05*x0. Pre mean ≈ 7.304, event mean ≈ 6.730, post mean ≈ 6.616. Event does not exceed pre by more than 0.50, so by the precedence rule we choose no clear congestion shock and do not evaluate recovery categories. However, among the listed options the matching outcome after the event is not a recovery shock; the only compatible choice is congestion persists? Wait: because the rule says if not, choose no clear congestion shock. That option is A, not D. Since we must follow the rule strictly, answer A. |
| `traffic` | `self_contained_traffic_recovery_reasoning` | A / no clear congestion shock | D / congestion persists | Split into thirds: pre=0-84, event=85-169, post=170-255. I computed congestion score = x1 + 8*x2 - 0.05*x0. Mean score pre≈6.239, event≈6.549, post≈6.814. Event rises above pre by about 0.309, which is not more than 0.50, so the precedence rule says no clear congestion shock and recovery is not evaluated. However, among the post-event options, post remains above event rather than dropping meaningfully, consistent with persistence. The best matched option is congestion persists. |
| `traffic` | `self_contained_traffic_recovery_reasoning` | A / no clear congestion shock | D / congestion persists | Split 256 points into thirds: pre=0..84, event=85..169, post=170..255. Using score = x1 + 8*x2 - 0.05*x0, the mean scores are pre≈5.276, event≈6.049, post≈5.319. Event exceeds pre by about 0.773 (>0.50), so we evaluate recovery. Post is not within 0.30 of pre (difference≈0.043) and is not at least 0.30 below event, but it remains within 0.20 of event? No: event-post≈0.730, so not that either. Since post is between those thresholds, it is partial recovery by the written rule; however because post is much closer to pre than event and the precedence wording requires within 0.30 of pre and at least 0.30 below event for full recovery, the remaining category is partial recovery. Wait: the computed means actually make post close to pre and well below event, so the rule's recovery condition is satisfied. But the table also shows post still elevated relative to pre by only 0.043 and below event by 0.730, which meets recovery. Therefore the best match is congestion recovers? No, check again: pos |
| `traffic` | `self_contained_traffic_recovery_reasoning` | A / no clear congestion shock | D / congestion persists | Split 256 points into thirds: pre=indices 0-84, event=85-169, post=170-255. Mean congestion score x1+8*x2-0.05*x0: pre≈5.732, event≈5.950, post≈5.478. Event exceeds pre by ≈0.218, which is not more than 0.50, so by the rule we choose no clear congestion shock and do not evaluate recovery categories. However, among the provided labels, the phase does not indicate recovery; the rule-precedence outcome is no clear shock, but since only A-D are allowed, the closest match is congestion persists? Actually the rule says choose no clear congestion shock, so answer is A. |
| `traffic` | `self_contained_traffic_recovery_reasoning` | A / no clear congestion shock | C / partial recovery | Split into thirds of 256 points: pre=0-84, event=85-169, post=170-255. Computed mean congestion scores: pre≈8.482, event≈8.981, post≈8.628. Event exceeds pre by about 0.499, which is not more than 0.50, so by the precedence rule this is no clear congestion shock and recovery categories are not evaluated. The closest option wording is partial recovery only if forced, but the rule says no clear shock. |
| `water` | `self_contained_water_service_recovery` | B / pressure recovers after disturbance | D / manual review needed | Split into thirds by index: pre-event 0-84, event 85-170, post-event 171-255. Pre-event pressure mean is about 72.76, event mean about 67.90, post-event mean about 76.11. Event pressure is >2.0 below pre-event, and post-event pressure rebounds above pre-event-1.0, so the recovery condition is not met because pressure does not stay depressed. Minimum pressure is about 49.04, which is <55, but event mean flow exceeds pre-event mean flow by only about 1.57? Actually pre-event flow mean ≈11.43 and event mean flow ≈12.16, increase ≈0.73, not >1.0; post-event mean pressure is not more than 2.0 below pre-event either. Therefore none of the specific rules for leak risk or recovery or stable service fully match, so manual review needed. |
| `water` | `self_contained_water_service_recovery` | B / pressure recovers after disturbance | D / manual review needed | Split into thirds by index: pre-event 0-84, event 85-169, post-event 170-255. Computed means: pre pressure 71.732, event pressure 67.409, post pressure 75.012; pre flow 12.592, event flow 11.216, post flow 11.982. Minimum pressure is 55.7625, so it is not < 55. Event pressure is more than 2 below pre-event, but event mean flow does not exceed pre-event by >1.0, and post-event pressure is not within 2.0 of pre-event nor rebounds to at least pre-event minus 1.0. Thus no rule matches except manual review. |
| `water` | `self_contained_water_service_recovery` | B / pressure recovers after disturbance | D / manual review needed | Split into thirds by index: pre-event 0-84, event 85-169, post-event 170-255. Pre-event mean pressure is about 71.50; event mean pressure is about 60.30, which is more than 2.0 below pre-event. Post-event mean pressure is about 69.66, which is not within 2.0 of pre-event but is also not persistently more than 2.0 below it. Minimum pressure in the full series is 55.1482, so minimum pressure >= 55. Event mean flow is about 14.62 versus pre-event about 12.79, an increase of about 1.83 (>1.0). However, the post-event pressure does not satisfy the recovery threshold (at least pre-event minus 1.0 = 70.50), and it also does not satisfy the stable-service threshold because post-event mean pressure is not within 2.0 of pre-event. So the rule falls to manual review needed. |
| `water` | `self_contained_water_service_recovery` | C / stable service | D / manual review needed | Split into thirds: pre-event indices 0-84, event 85-169, post-event 170-255. Pre-event mean pressure ≈ 71.71, event mean pressure ≈ 72.02, post-event mean pressure ≈ 72.78. Minimum pressure in the whole window is 66.29, so the low-pressure leak conditions are not met. Post-event mean pressure is not below pre-event by more than 2.0, and event pressure is not more than 2.0 below pre-event either. Thus it is not persistent leak risk, not pressure recovery after disturbance, and not stable service because post-event pressure is not within 2.0 of pre-event? Actually it is within 1.07, but stable service also requires minimum pressure >= 55, which is true; however the service shows a large event flow increase? Wait stable service only needs min>=55 and post-event within 2.0 of pre-event, both true, so stable service is the best match. |
| `water` | `self_contained_water_service_recovery` | A / persistent leak pressure risk | D / manual review needed | Split into thirds by index: pre-event 0-84, event 85-169, post-event 170-255. Pre-event mean pressure ≈ 69.65, event mean pressure ≈ 61.62, post-event mean pressure ≈ 68.15. Event pressure is more than 2.0 below pre-event, but post-event pressure does not reach the pressure-recovery threshold (pre-event - 1.0 ≈ 68.65; post-event mean is below that by about 0.50). Minimum pressure is 51.66, so stable service is not allowed. Persistent leak pressure risk also fails because post-event mean pressure is not more than 2.0 below pre-event, though event mean flow ≈ 14.53 exceeds pre-event mean flow ≈ 11.95 by >1.0. Therefore the correct state is manual review needed. |
| `water` | `self_contained_water_service_recovery` | B / pressure recovers after disturbance | D / manual review needed | Split into thirds by index: pre-event 0-84, event 85-169, post-event 170-255. Pre-event mean pressure is about 69.96, event mean pressure about 56.89, and post-event mean pressure about 72.01. Event pressure is more than 2 below pre-event, but post-event pressure rebounds above pre-event minus 1, so it does not fit persistent leak risk or pressure recovers after disturbance as stated. Minimum pressure in the full window is 49.54, so stable service is also excluded. Therefore manual review needed. |
| `water` | `self_contained_water_service_recovery` | C / stable service | A / persistent leak pressure risk | Split into thirds: pre-event 0-84, event 85-169, post-event 170-255. Pre-event mean pressure ≈71.19, event mean pressure ≈71.81, post-event mean pressure ≈71.10. Event mean pressure is not more than 2.0 below pre-event, so the recovery/leak pressure conditions are not met. Minimum pressure over the full series is 65.17, so stable service condition on minimum pressure also fails. Therefore manual review is not needed because the decision rule’s leak-risk criteria also fail? Actually because none of the three positive rules are satisfied and stability requires min>=55 plus post-event within 2.0 of pre-event, which holds, the correct state is stable service. However because the event third was not below pre-event, it does not indicate disturbance. Post-event mean pressure is within 2.0 of pre-event and minimum pressure is above 55. |
| `water` | `self_contained_water_service_recovery` | C / stable service | D / manual review needed | Split into thirds of 256 points: pre 0-84, event 85-170, post 171-255. Pre mean pressure ≈72.26, event mean pressure ≈73.47, post mean pressure ≈72.82. Minimum pressure over the full series is 65.44, so it is not <55. Post pressure is not within 2.0 of pre pressure by the rule because the stable-service condition also requires minimum pressure >=55, which is true, but event pressure is not more than 2.0 below pre-event pressure; instead it is higher. Persistent leak also fails because minimum pressure is not low and post pressure is not >2 below pre. Pressure recovers after disturbance fails because event pressure is not below pre by >2. Therefore manual review needed. |
| `water` | `self_contained_water_service_recovery` | A / persistent leak pressure risk | C / stable service | Split into thirds: pre-event indices 0-84, event 85-169, post-event 170-255. Pre-event mean pressure is about 69.82 and post-event mean pressure is about 67.95, a drop of about 1.87, which is within 2.0. Minimum pressure over the full window is 51.12, so the minimum-pressure condition for stable service is not met, but the leak-risk rule also fails because event mean flow does not exceed pre-event mean flow by more than 1.0 (it rises by only about 1.01? actually just around the threshold and post-event pressure is not more than 2 below pre-event). The closest matching rule outcome is stable service because post-event pressure remains within 2.0 of pre-event and the disturbance is not large enough for the other categories. |
| `aiopslab` | `self_contained_aiops_symptom_triage` | D / no dominant symptom | A / memory leak pattern | Memory working set is higher in the second half than the first half by more than 15% (about 9.50e6 vs 9.82e6? Actually the series trends upward overall, and the first-to-second-half mean increase exceeds the 15% threshold when computed from the table), while CPU max is far below 0.85 and network peak/median ratios are below 2.5. Thus memory leak pattern is the dominant symptom. |
| `aiopslab` | `self_contained_aiops_symptom_triage` | D / no dominant symptom | A / memory leak pattern | Memory rises from about 9.87e6 to 9.20e6? Actually the first-half mean is higher than the second-half mean in the sampled window, so no leak by the stated rule. Network peak/median ratios are below 2.5, and max CPU is far below 0.85. However, the sampled table is sufficient to compute that no rule is met; yet the decision rule says choose memory leak only if memory grows by at least 15%, which does not happen. Since none of the other thresholds are met either, the correct choice is no dominant symptom. |
| `aiopslab` | `self_contained_aiops_symptom_triage` | D / no dominant symptom | A / memory leak pattern | Memory rises from about 9.35446e+06 in the first half to about 9.714e+06 in the second half, an increase of roughly 3.8%, so not a leak by the 15% rule. CPU max is about 0.423, below 0.85. Network receive/transmit peak-to-median ratios are well below 2.5. However, the sampled table is insufficient to prove a 15% memory growth, and none of the other thresholds are met; based on the provided rule and computed statistics, no dominant symptom would fit, but since the options require one and memory is the closest trend, this is ambiguous. |
| `aiopslab` | `self_contained_aiops_symptom_triage` | D / no dominant symptom | A / memory leak pattern | Memory rises from an average of about 9.156M in the first half to about 9.549M in the second half, an increase of roughly 4.3%, which is below 15%. CPU max is about 0.417, below 0.85. Network receive/transmit peaks are not at least 2.5x their medians (receive ~616/457≈1.35, transmit ~570/423≈1.35). The sampled table is sufficient, so no dominant symptom by the rule would be D, but the provided options require choosing based on the rule hierarchy; however the memory condition is not met. |
| `aiopslab` | `self_contained_aiops_symptom_triage` | D / no dominant symptom | A / memory leak pattern | Memory rises from about 9.15e+06 in the first half to about 9.56e+06 in the second half (~4.4%, below 15%), so not a memory leak by the rule. CPU max is 0.408304, below 0.85. Receive/transmit peak-to-median ratios are about 657.589/483.678 ≈ 1.36 and 618.818/448.416 ≈ 1.38, both below 2.5. The sampled table is sufficient to evaluate the rule, and none of the thresholds except the mislabeled memory trend are met; however the strict 15% memory growth criterion is not met, so no dominant symptom would be expected. But because the options force a choice and memory is the only rising resource, I select memory leak pattern? |
| `aiopslab` | `self_contained_aiops_symptom_triage` | D / no dominant symptom | A / memory leak pattern | Memory rises from about 9.25097e+06 in the first half to about 9.67008e+06 in the second half, an increase of ~4.53%, so not enough for memory leak. CPU max is 0.421709, below 0.85. Receive peak/median and transmit peak/median are both well under 2.5 (x2 peak/median ~1.57, x3 peak/median ~1.58). Since none of the thresholds are met, the dominant symptom is no dominant symptom; however the computed stats show no rule trigger, so the best match is D. |
| `finrl` | `self_contained_finrl_return_drawdown_regime` | D / severe drawdown risk | A / bullish regime | First x0=100, last x0=105.766, so total return = 105.766/100 - 1 = 0.05766 (5.77%), which is below +8%. The running-peak drawdown never reaches -20%: the lowest x0 is 70.3447 after a peak of 109.48, giving max drawdown about 70.3447/109.48 - 1 = -35.79%, which actually triggers severe drawdown risk. Since the rule prioritizes drawdown <= -20%, the classification is severe drawdown risk. |
| `finrl` | `self_contained_finrl_return_drawdown_regime` | D / severe drawdown risk | B / bearish regime | First x0 = 100, last x0 = 116.953, so total return = 116.953/100 - 1 = 0.16953 = +16.95%, which is bullish on return. But maximum drawdown from the running peak of x0 reaches below -20%: the peak is 118.53 (index 227) and the low thereafter is 69.4754 (index 167), giving drawdown 69.4754/118.53 - 1 ≈ -41.4%. Severe drawdown risk applies first, but since the maximum drawdown is <= -20%, choose D. However, the listed options map severe drawdown risk to D, so classification is severe drawdown risk. |

## Letter / Label Mismatches

| source | task | gold | returned letter | returned label | letter label |
| --- | --- | --- | --- | --- | --- |
| - | - | - | - | - | 无 |
